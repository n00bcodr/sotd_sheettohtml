name: Pull data from sheet and update HTML

on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'     
        required: true
        default: 'warning'
      tags:
        description: 'Update HTML'
runs:
  using: 'node16'
  main: 'src/migrate.js'
